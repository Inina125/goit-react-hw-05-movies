import styled from 'styled-components';

const Cont = styled.ul`
  display: flex;
  flex-direction: column;
`;

const Item = styled.li`
  display: flex;
  flex-direction: column;
  margin: 10px;
`;

const Name = styled.p`
  font-weight: 600;
`;

const ReviewText = styled.p``;

export { Cont, Item, Name, ReviewText };
